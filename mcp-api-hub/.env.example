# Copy to .env and customize as needed
# Global defaults
DEFAULT_TIMEOUT=60
RETRY_MAX=3
LOG_LEVEL=INFO

# Flask API base (defaults to local Flask on 8088)
BPM_URL=http://localhost:8088
# Optional credentials for the underlying BPM system (if your Flask uses them)
# BPM_USERNAME=
# BPM_PASSWORD=
